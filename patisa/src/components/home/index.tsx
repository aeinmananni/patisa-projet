import WindowsMode from "./windows-mode";
import MobileMode from "../../component-mobileMode/mobile-mode";

const Home = () => {
  return (
    <>
      <WindowsMode />
      <MobileMode />
    </>
  );
};

export default Home;
